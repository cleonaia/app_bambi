// BrandColors.swift
// Define los colores principales de la marca BAMBI para uso en toda la app.
import SwiftUI

struct BrandColors {
    static let primary = Color("BambiPrimary") // Rojo BAMBI
    static let secondary = Color("BambiSecondary") // Amarillo BAMBI
    static let accent = Color("BambiAccent") // Azul BAMBI
    // Agrega más colores según el branding
}
